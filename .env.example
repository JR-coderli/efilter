## 是否本地开发环境
IS_LOCAL=true

## PostgreSQL 数据库连接信息
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=admin123
DB_NAME=risk_engine
DB_SSL_MODE=disable
DB_TIMEZONE=Asia/Shanghai

## Redis 连接信息
## 使用独立的 db，避免与其他项目共用 db0 导致 key 冲突或被 flush 影响
REDIS_ADDR=127.0.0.1:6379
REDIS_PASSWORD=
REDIS_DB=5

## 服务端口
APP_PORT=8080

## API Key（开发默认）
API_KEY=risk-engine-dev-key-2026

## IP 数据库下载地址（用于 tools/update-ipdb/update-ipdb.sh 自动更新）
## 请将 YOUR_TOKEN 替换为实际 token
## 注意：URL 必须加双引号，否则 shell source 时 & 会被解析为后台运行符
IP2LOCATION_URL="https://www.ip2location.com/download?token=YOUR_TOKEN&file=DB1LITEBINIPV6"
IP2PROXY_URL="https://www.ip2location.com/download?token=YOUR_TOKEN&file=PX2LITEBIN"
IP2PROXY_IPv6_URL="https://www.ip2location.com/download?token=YOUR_TOKEN&file=PX2LITECSVIPV6"

## IP 数据库路径（相对路径，以 risk-engine 工作目录为基准）
IP2LOCATION_PATH=../../binfiles/IP2LOCATION-LITE-DB1.IPV6.BIN/IP2LOCATION-LITE-DB1.IPV6.BIN
IP2PROXY_PATH=../../binfiles/IP2PROXY-LITE-PX2.BIN/IP2PROXY-LITE-PX2.BIN

## IP2Proxy IPv6 CSV 路径（LITE 版只有 PUB 类型，用于 IPv6 fallback）
IP2PROXY_IPV6_CSV_PATH=../../binfiles/IP2PROXY-LITE-PX2.IPV6.CSV/IP2PROXY-LITE-PX2.IPV6.CSV

## 限流配置（QPS 限流：窗口 1 秒，最大请求 2000）
RATE_LIMIT_WINDOW=1
RATE_LIMIT_MAX_REQUESTS=2000

## 日志级别
LOG_LEVEL=info
